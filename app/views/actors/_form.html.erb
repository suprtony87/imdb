
<% if @actor.errors.any? %>
  Please fix the following errors:
  <% @actor.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>

<%= form_for @actor do |f| %>

<%= f.label :name, 'Actors Name??' %>
</br>
<%= f.text_field :name %>

</br>
</br>

<%= f.label :year_of_birth, 'when were they born (year)' %>
</br>
<%= f.text_field :year_of_birth %>

</br>
</br>

<%= f.label :thumbnail, 'Put a thumbnail pic' %>
</br>
<%= f.text_field :thumbnail %>


<%= f.submit %>
<% end %>