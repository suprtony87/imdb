<h2 class='text-center'>Our Current Actors</h2>
<hr>
<br>
<div class="row text-center">
  <% @actors.each do |actor| %>
    <div class="col-md-4">
      <%= link_to(image_tag(actor.thumbnail), actor_path(actor)) %>
      <p><%= actor.name %></p>
    </div>
<% end %>
</div>
